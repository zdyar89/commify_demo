version: '3.9'

services:
  myapp:
    build:
      context: .
      dockerfile: ./tax_demo/dockerfile
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    ports:
      - "8080:8080"
      - "8081:8081"
